name: signallost
description: Emergency SOS app using Bluetooth mesh networking — no internet required.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk:         flutter

  geolocator: ^10.0.0
  permission_handler: ^12.0.1
  flutter_bluetooth_serial: ^0.4.0   # Bluetooth mesh

  # Mesh networking & internet gateway:
  nearby_connections: ^4.0.0         # Google Nearby (BT + Wi-Fi P2P unified)
  flutter_reactive_ble: ^5.4.1        # BLE scanning/advertising fallback
  connectivity_plus: ^6.0.0           # Internet availability detection
  http: ^1.1.0                        # HTTP POST to backend
  device_info_plus: ^10.0.0           # Device ID (brand + model)
  uuid: ^4.0.0                        # Unique ID generation

  # Android — add to AndroidManifest.xml:
  #   <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
  #   <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
  #   <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
  #   <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
  #       android:usesPermissionFlags="neverForLocation"/>  <!-- API 33+ -->
  #
  # iOS — add to Info.plist:
  #   NSLocalNetworkUsageDescription — for MultipeerConnectivity (Wi-Fi P2P)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
